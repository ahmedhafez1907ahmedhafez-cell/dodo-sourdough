# الوضع الحالي

## تسجيل الدخول — بقى Firebase Auth ✅

شلت كود الـ ٦ أرقام بتاع Resend خالص (كان محتاج دومين مدفوع)، وحوّلت
حسابات العملاء لـ **Firebase Auth** — نفس اللي بتستخدمه للأدمن.

**مجاني تماماً**، ومش محتاج تشتري دومين، وإيميلات التفعيل وإعادة تعيين
كلمة السر بتتبعت من سيرفرات Google نفسها.

### الإعداد ✅ اتأكدنا منه

- المشروع: `dodo-sourdough-e4a30`
- **Email/Password: Enabled** ✅
- الخطة: **Spark (No-cost $0/month)** — مفيش أي تكلفة

جاهز يشتغل على طول.

> ملحوظة: في مشروعين في الكونسول باسم متشابه — `dodo-sourdough-e4a30`
> (ده اللي الموقع شغال عليه، صح) و`dodo-sourdough` (فاضي/قديم).
> خلي بالك ما تخبطش بينهم لما تدخل الكونسول.

### اللي اتحسّن

| | قبل | بعد |
|---|---|---|
| مكان الحسابات | المتصفح (localStorage) | سيرفرات Firebase |
| الباسورد | **نص صريح** | متشفّر، مبيوصلش للمتصفح |
| نسيت كلمة السر | مفيش | لينك على الإيميل |
| تفعيل الإيميل | مفيش | تلقائي عند التسجيل |
| الحساب على أجهزة تانية | مبيشتغلش | بيشتغل |

قبل كده أي حد يفتح DevTools كان يشوف باسوردات كل اللي سجّلوا على نفس
الجهاز. دي كانت أخطر حاجة في الموقع.

### حاجات صغيرة

- الصورة الشخصية بتتحفظ محلياً مش في Firebase — لأنها base64 وطولها
  آلاف الحروف و`photoURL` محدود بـ 2048 حرف وكان هيرمي خطأ
- رسائل أخطاء Firebase الإنجليزية اتترجمت لعربي مفهوم
- **الحسابات القديمة اللي في المتصفح مش هتشتغل** — اللي سجّل قبل كده
  لازم يعمل حساب جديد. مفيش طريقة ننقلهم لأن الباسوردات كانت محلية بس.
- التعليقات متأثرتش — هي أصلاً مش محتاجة تسجيل دخول

---

## باجات اتصلحت في الأوردرات

### 🔴 الأسعار كانت بتتاخد من العميل
`itemsTotal` كان بيتحسب من `i.totalPrice` الجاي من المتصفح. أي حد يفتح
DevTools ويعدّل الطلب قبل ما يبعته كان يشتري رغيف ٣٥٠ بجنيه.

**اتصلح:** `lib/pricing.js` بيعيد حساب كل حاجة من Firestore، وبيتأكد إن
المنتج موجود و`active`، والإضافات مسموحة، والكمية معقولة، وسعر الخميرة
مضاعف صحيح لسعر الجرام.

### 🟠 شحنة بمبلغ ناقص
لو العميل اختار "محافظة تانية" (التوصيل بيتحدد بالتليفون)، الشحنة كانت
بتتبعت لمايلرز بمبلغ من غير التوصيل — المندوب كان هيحصّل أقل.

**اتصلح:** الشحنة مبقتش تتبعت في الحالة دي، تعملها يدوي بعد الاتفاق.

### 🟠 التصنيف عند مايلرز
`itemCategoryFor` بيقرا `catalog` عشان يفرّق خبز من أدوات، بس الكارت
مكنش بيبعت الحقل ده. **اتصلح.**

### 🟡 أخطاء مايلرز كانت بتضيع
بقت تتسجل في الأوردر نفسه (`mylerzError`) مع `mylerzPickupCode`.

---

## مايلرز — تشخيص الـ UNAUTHORIZED

اختبرت من جوه بوابة مايلرز نفسها (بجافاسكريبت في الصفحة) وطلعت النتايج دي:

**١) التوكن بتاعنا سليم تماماً.** جبت توكن البوابة (3051 حرف) وقارنته
بتوكن السكريبت — الاتنين اداروا **نفس النتيجة بالظبط** على نفس الطلب.
فالمشكلة مش في المصادقة ولا في طريقة تسجيل الدخول.

**٢) الصلاحية بتعدّي فعلاً.** لما بعتنا package فاضي `[{}]` الرد كان
`NOT_FOUND` مش `UNAUTHORIZED` — يعني الـ endpoint مفتوح لينا.

**٣) الحقل المسبّب اتحدد: `MerchantId`**

| اللي بعتناه | الرد |
|---|---|
| `{}` | NOT_FOUND |
| `{ WarehouseId: 40876 }` | NOT_FOUND |
| `{ MerchantId: 999 }` (رقم غلط) | NOT_FOUND |
| **`{ MerchantId: 23085 }`** (الصح) | **UNAUTHORIZED** |

لاحظ العكس: الرقم **الغلط** بيعدّي، والرقم **الصح** بيترفض. تفسير ده إن
السيرفر بيلاقي التاجر 23085 موجود، وبعدين بيسأل «هل اليوزر ده مسموح له
يعمل شحنات **نيابة عن** التاجر ده؟» ويرفض. الرقم الغلط مبيوصلش للسؤال.

**٤) البوابة بتستخدم نفس السيرفر ونفس المسار.**
`https://api.mylerz.net` + `package/SavePackageList` — طلعتهم من كود
البوابة نفسه. فمفيش endpoint سري احنا مش عارفينه.

### الخلاصة
الـ API غالباً بيحدد التاجر من التوكن لوحده، وإحنا بنبعت `MerchantId`
صراحة فبيفهمها انتحال صفة. بس لما شلناه الرد بقى `NOT_FOUND` — يعني
في حقل تاني بيحدد التاجر إحنا مش عارفينه (يمكن `SubscriberId` أو
`MerchantCode`).

**٥) جربت كل البدائل لتعريف التاجر — كلها فشلت:**
`SubscriberId` / `MerchantCode` / `MerchantName` / `WarehouseName` /
`HubEnterPoint` / `PackageSourceId` — كلهم رجّعوا `NOT_FOUND`.

يعني الـ API **لازم** ياخد `MerchantId`، وأول ما ياخده صح بيقول
`UNAUTHORIZED`. ده جدار صلاحيات حقيقي مش مشكلة في شكل البيانات.

### الخطوة الحاسمة الجاية
لازم نشوف طلب حقيقي من البوابة. اعمل شحنة **واحدة** من
`mylerz.net` → **Add Packages**، وقبل ما تضغط Save افتح
**F12 → Network**. بعد الحفظ دوّر على `SavePackageList` واعمل
**Copy → Copy as cURL** وابعتهولي.

ده هيدينا الـ payload الصح بالظبط وننسخه، وساعتها التكامل يخلص فوراً.
(الشحنة دي حقيقية — الغيها بعدها على طول.)

---

## مايلرز — باقي الإعداد

كل حاجة جاهزة: السيرفر `api.mylerz.net`، المسار `/api/package/SavePackageList`،
MerchantId `23085`، WarehouseId `40876`، الترميز كله متطابق.

**`MYLERZ_ENABLED` لسه `false` عن قصد.** فاضل تعمل:

1. `node scripts/testShipment.js --send` (وانت قدام الكمبيوتر)
2. خد الـ Barcode → ادخل `mylerz.net` → Packages
3. **بص على مبلغ الـ COD — المفروض ٣٥٠**
4. الغي الشحنة
5. لو المبلغ صح → `MYLERZ_ENABLED=true`

مبلغ الـ COD هو آخر حاجة مش متأكد منها — الـ schema مفيهوش حقل اسمه
`COD_Value`، فبعتّه في `ValueOfGoods`. لو طلع صفر قوللي.

### سؤال محتاج قرارك
الكود بيبعت شحنة مايلرز لأوردرات **بنها** كمان، مع إن مخزنك في بنها
وعندك أسعار توصيل محلية منفصلة. لو بتوصّلها بنفسك يبقى بتدفع شحن على
غير لزوم. لو عايز توقفها:
```
MYLERZ_SKIP_BANHA=true
```

---

## فاضل

- [x] ~~تفعيل Email/Password في Firebase~~ — مفعّل بالفعل ✅
- [ ] شحنة تجريبية + تأكيد مبلغ COD
- [ ] `MYLERZ_ENABLED=true` بعدها
- [ ] قرار `MYLERZ_SKIP_BANHA`
- [ ] تغيير باسوردات مايلرز (ظهرت في اللقطات وواتساب)
- [ ] رفع على جيت هاب
